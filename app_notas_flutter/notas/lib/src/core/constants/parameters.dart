enum NoteType {
  Text,
  Image,
  Network,
  TExtImage,
  TextNetwork,
  ImageNetwork,
  TextImageNetwork
}

enum NoteState { Visisble, Archived, Deleted }

enum TaskState { Done, Review, Create, PastDate }

enum InternetServices { Connected, Disconnected, Exception }
